# opencode.ai 整域直连
# 背景：2026-08-26 排查确认本机使用的 subconverter v0.9.0 会静默丢弃 ruleset 中的 URL-REGEX 规则，
#       导致"仅 API endpoint 直连"的方案无法生效（规则不进入生成的 Clash 配置）。
#       故退回用户预先授权的方案：整个 opencode.ai 域名直连。
# 影响：浏览器访问 opencode.ai 官网 / 登录验证 也会直连，不再走代理。
# 如日后 subconverter 支持 URL-REGEX，可改回按 /zen/go/v1/... 路径分流。
DOMAIN-SUFFIX,opencode.ai
